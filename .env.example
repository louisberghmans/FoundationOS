# Host port for the local Docker deployment.
FOUNDATION_OS_PORT=8080

# Set both values to true when FoundationOS is behind a trusted HTTPS reverse proxy.
FOUNDATION_OS_COOKIE_SECURE=false
FOUNDATION_OS_TRUST_PROXY=false
